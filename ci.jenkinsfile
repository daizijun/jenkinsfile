pipeline{
    agent { node { label "jenkins" } }
    
    stages{
        stage("build"){
         stages{
            script{
                mvnHome = tool "M2"
                sh "${mvnHome}/bin/mvn -v"
            }
         }
        }
    }
}
